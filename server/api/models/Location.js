'use strict'

class Location {
  constructor(id, name, address, lattitude, longitude) {
    this.id = id
    this.name = name
    this.address = address
    this.lattitude = lattitude
    this.longitude = longitude
  }
}

export default Location
